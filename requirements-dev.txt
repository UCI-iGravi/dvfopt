# Development dependencies — includes everything from pyproject.toml plus
# extras for notebooks (pandas, ipykernel) and benchmark registration methods.
# For the minimal install use: pip install -e .
# For benchmarks only:        pip install -e ".[benchmarks]"

# Core scientific stack
numpy
scipy>=1.15,<1.19   # keep in lockstep with pyproject [project.dependencies]
matplotlib
pandas
SimpleITK
nibabel
ipykernel

# VoxelMorph (install from GitHub — PyPI version has Python 3.11+ bug)
# neurite is pulled automatically by voxelmorph (dev branch)
voxelmorph @ git+https://github.com/voxelmorph/voxelmorph.git

# Elastix registration (ITK wrapper)
itk-elastix

# OpenCV optical flow
opencv-python-headless

# TransMorph (Swin Transformer backbone)
timm

# Progress bars — used directly by benchmarks/registration/transmorph-registration.ipynb
tqdm

# OSQP — the elastic-QP inner for the windowed engine (dvfopt[solvers])
osqp

# Clarabel — interior-point leg of the windowed engine's hybrid QP backend
# (core dependency; keep in lockstep with pyproject [project.dependencies])
clarabel>=0.9
